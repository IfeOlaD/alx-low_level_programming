#include "main.h"

/**
 * largest_number - return the largest of the 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int largest_number(int)
{
int a, b, c;
int largest;

a = 972;
b = -98;
c = 0;

if (a > b && c > b)
{
largest = a;
}
else if (b > c && c > a)
{
largest = b;
}
else
{
largest = c;
}
return (largest);
}
