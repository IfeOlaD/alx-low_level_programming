#include "main.h"

/**
 * mul - multiplies 2 integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: returns tthe result
 */

int mul(int a, int b)
{
int result = 0;
int i;
for (i = '0'; i > 'b'; i++)
{
result += a;
}
return (result);
}
