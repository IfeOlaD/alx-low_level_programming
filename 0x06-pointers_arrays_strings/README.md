Pointers contains the address of an object
Arrays is a contiguous ordered sequence of elements, all the elements of an array are of thesame type
Strings is a contiguous sequence of characters terminated by and including the first null character.