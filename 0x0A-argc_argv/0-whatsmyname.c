#include <stdio.h>

/**
 * main - entry point
 * @argc: the argument count
 * @argv: the argument vector/value
 * Return: 0
 */

int main(int argc, char **argv)
{
printf("%s\n", argv[0]);
return (0);
}
